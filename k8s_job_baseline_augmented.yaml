apiVersion: batch/v1
kind: Job
metadata:
  name: steer-train-job-baseline-augmented
  namespace: gp-engine-mizzou-dcps
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: steer-train-baseline-augmented
    spec:
      restartPolicy: Never
      # Request A100 GPUs (or GPUs with >24GB VRAM)
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: nvidia.com/gpu.product
                operator: In
                values:
                - NVIDIA-A100-SXM4-80GB
                - NVIDIA-A100-80GB-PCIe
      tolerations:
      - key: "nautilus.io/reservation"
        operator: "Equal"
        value: "mizzou"
        effect: "NoSchedule"
      containers:
      - name: steer-train
        image: khurramkhalil/steer:latest
        imagePullPolicy: Always
        workingDir: /workspace/STEER
        # Command:
        # 1. Generate augmented dataset
        # 2. Run distributed training with torchrun
        command: ["/bin/bash", "-c"]
        args:
          - |
            echo "Generating augmented dataset..."
            python dataset/build_sudoku_dataset.py --output-dir data/sudoku-extreme-1k-aug-1000 --subsample-size 1000 --num-aug 1000 && \
            echo "Starting training..." && \
            torchrun --nproc_per_node=4 pretrain.py \
            arch=trm \
            data_paths="[data/sudoku-extreme-1k-aug-1000]" \
            evaluators="[]" \
            epochs=50000 eval_interval=1000 \
            lr=4e-4 puzzle_emb_lr=4e-4 weight_decay=1.0 puzzle_emb_weight_decay=1.0 \
            arch.mlp_t=True arch.pos_encodings=none \
            arch.L_layers=2 \
            arch.H_cycles=3 arch.L_cycles=6 \
            +steer_lambda=0.0 \
            +steer_epsilon_viol=0.1 \
            +steer_epsilon_stab=0.01 \
            global_batch_size=512 \
            +run_name="L0.0_Baseline_Augmented" \
            +project_name="STEER_PAPER_AUGMENTED" \
            ema=True
        resources:
          requests:
            nvidia.com/a100: 4
            memory: "32Gi"
            cpu: "16"
          limits:
            nvidia.com/a100: 4
            memory: "48Gi"
            cpu: "24"
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        envFrom:
        - secretRef:
            name: steer-secrets
        # Shared memory fix for PyTorch DDP
        volumeMounts:
          - mountPath: /dev/shm
            name: dshm
      volumes:
        - name: dshm
          emptyDir:
            medium: Memory
