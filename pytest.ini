[pytest]
testpaths = tests
# The hydra pytest plugin is auto-registered but not needed for the unit tests,
# and fails to import in some environments. Disable it so `pytest` runs cleanly.
addopts = -p no:hydra_pytest
